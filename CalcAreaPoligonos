###Calcular a área do quadrado
L = float(input('Digite a lateral do quadrado: '))
R = L**2 
print ('A área do quadrado é', R)

###Calcular a área do triângulo
B = float(input('Digite a base do triângulo: ' ))
h = float(input('Digite a altura do triânguangulo: ') )
A = (B*h)/2
print ('A área do triângulo é', A )

#Calcular o Raio da circunferência (Área = π*r²)
L = int(input('Digite o valor do lado do quadrado:'))
R = L**2 * ( 1 - 3,14/4 )
print ('O raio mede: ', R)
